generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Favourite {
  id           Int    @id @default(autoincrement())
  title        String
  url          String
  release_date String
}
